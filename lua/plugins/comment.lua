return {
  {
  "numToStr/Comment.nvim",
  opts = {
    -- configuração padrão funciona bem
    -- | Modo   | Ação                                   | Atalho                           |
    -- | ------ | -------------------------------------- | -------------------------------- |
    -- | Normal | Comentar a linha atual                 | `gcc`                            |
    -- | Normal | Comentar várias linhas (com movimento) | `gc3j` (comenta 3 linhas abaixo) |
    -- | Visual | Comentar seleção                       | `gc`                             |
    -- | Visual | Comentar em bloco (se suportado)       | `gb`                             |
  },
  lazy = false,
}
}

